{
  "name": "claude-canvas",
  "version": "0.2.0",
  "type": "module",
  "private": true,
  "workspaces": [
    "canvas"
  ],
  "scripts": {
    "start": "bun run canvas/src/cli.ts",
    "show": "bun run canvas/src/cli.ts show",
    "spawn": "bun run canvas/src/cli.ts spawn",
    "build": "bun run scripts/build.ts",
    "build:check": "bun run scripts/check-bundle.ts",
    "check:standalone": "bun run scripts/check-standalone.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/react": "^19.2.7",
    "@types/react-dom": "^19.2.3"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "commander": "^14.0.2",
    "ink": "^6.6.0",
    "ink-spinner": "^5.0.0"
  }
}
