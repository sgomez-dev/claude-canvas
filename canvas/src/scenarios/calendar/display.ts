import type { ScenarioDefinition } from "../types";

export const displayScenario: ScenarioDefinition = {
  name: "display",
  description: "View-only calendar display",
  canvasKind: "calendar",
  interactionMode: "view-only",
};
